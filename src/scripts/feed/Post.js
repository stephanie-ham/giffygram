
// file will eventually be used to add deletePost, favoritePost, dependent on logged in user. Probably use an if statement (if *variable* === )

export const Post = () => {


  return 'Individual Post'
}

